# OpenMetadata Configuration
OPENMETADATA_URL=http://dq_openmetadata:8585/api

# JWT Token — automatically injected by deploy.py (no manual step needed).
# If auto-retrieval fails, get it manually:
#   http://localhost:8585 > Settings > Bots > ingestion-bot > Copy JWT Token
OPENMETADATA_JWT_TOKEN=your_jwt_token_here

# Cluster (ne pas modifier)
CLUSTER_NAME=openmetadata-games

# Authentication (ne pas modifier)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin

# MySQL Database
MYSQL_ROOT_PASSWORD=openmetadata_root_pass_2026
MYSQL_DATABASE=openmetadata_db
MYSQL_USER=openmetadata_user
MYSQL_PASSWORD=openmetadata_user_pass_2026

# Airflow Database
AIRFLOW_DB=airflow_db
AIRFLOW_USER=airflow_user
AIRFLOW_PASSWORD=airflow_pass_2026

# Fernet Key for Airflow (must be 32 url-safe base64-encoded bytes)
# Valeur par défaut fournie pour simplifier le setup (OK pour dev/cours)
METADATA_FERNET_KEY=46BKJoQYlPPOexq0OhDZnIlNepKFf87WFwLbfzqDDho=

# Elasticsearch
ES_JAVA_OPTS=-Xms1024m -Xmx1024m

# Logging
LOG_LEVEL=INFO
